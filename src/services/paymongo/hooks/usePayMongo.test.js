// import { act, renderHook } from "@testing-library/react-hooks"

// import usePayMongo from "./usePayMongo"

test("Test paymongo service fee calculation", () => {
  // const { result } = renderHook(() => usePayMongo())
  // act(() => {
  //   result.current.calculatePaymongoFees({ amount: 1000 })
  // })
  // expect(result.current.calculatePaymongoFees({ amount: 1000 })).toBe(1050)
})
